<%- include('./partials/header') %>

<div class="w-full min-h-screen flex items-center justify-center px-20 bg-gray-50 dark:bg-zinc-900 transition-colors duration-300">
    <div class="p-10 rounded-lg shadow-md w-full max-w-md my-10 bg-white dark:bg-zinc-800 border border-gray-200 dark:border-zinc-700">
        
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-2xl font-bold text-black dark:text-white">Edit Profile</h2>
            <a href="/users/account" class="text-gray-500 dark:text-gray-400 hover:text-black dark:hover:text-white">
                <i class="ri-close-line text-2xl"></i>
            </a>
        </div>

        <form action="/users/update-profile" method="post" enctype="multipart/form-data" autocomplete="off">
            
            <div class="mb-6 flex flex-col items-center">
                <div class="w-24 h-24 rounded-full overflow-hidden mb-3 border dark:border-zinc-600 flex items-center justify-center bg-gray-100 dark:bg-zinc-700">
                    <% if(user.picture) { %>
                        <img id="profile-preview" class="w-full h-full object-cover" src="data:image/jpeg;base64,<%= user.picture.toString('base64') %>">
                    <% } else { %>
                        <img id="profile-preview" class="w-full h-full object-cover hidden" src=""> 
                        <span id="profile-placeholder" class="text-gray-400 text-xs">No Image</span>
                    <% } %>
                </div>
                
                <label class="cursor-pointer text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline">
                    Change Photo
                    <input type="file" name="image" class="hidden" onchange="previewImage(event)">
                </label>
            </div>

            <div class="mb-5">
                <label class="block text-sm font-medium mb-2 text-gray-700 dark:text-gray-300">Full Name</label>
                <input type="text" name="fullname" value="<%= user.fullname %>" 
                       class="w-full px-4 py-2 border rounded-md focus:outline-none focus:border-blue-500 bg-white border-gray-300 dark:bg-zinc-700 dark:border-zinc-600 dark:text-white">
            </div>

            <div class="mb-5">
                <label class="block text-sm font-medium mb-2 text-gray-700 dark:text-gray-300">Contact Number</label>
                <input type="number" name="contact" value="<%= user.contact || '' %>" placeholder="Phone number"
                       class="w-full px-4 py-2 border rounded-md focus:outline-none focus:border-blue-500 bg-white border-gray-300 dark:bg-zinc-700 dark:border-zinc-600 dark:text-white">
            </div>

            <div class="mb-5">
                <label class="block text-sm font-medium mb-2 text-gray-700 dark:text-gray-300">Address</label>
                <textarea name="address" rows="2" placeholder="House No, Street Area"
                       class="w-full px-4 py-2 border rounded-md focus:outline-none focus:border-blue-500 bg-white border-gray-300 dark:bg-zinc-700 dark:border-zinc-600 dark:text-white"><%= user.address || '' %></textarea>
            </div>

            <div class="mb-6">
                <label class="block text-sm font-medium mb-2 text-gray-700 dark:text-gray-300">Pincode</label>
                <input type="number" name="pincode" value="<%= user.pincode || '' %>" placeholder="110001"
                       class="w-full px-4 py-2 border rounded-md focus:outline-none focus:border-blue-500 bg-white border-gray-300 dark:bg-zinc-700 dark:border-zinc-600 dark:text-white">
            </div>
            
            <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white py-3 rounded-md font-semibold transition">
                Save Changes
            </button>
        </form>
    </div>
</div>

<script>
    function previewImage(event) {
        const input = event.target;
        const reader = new FileReader();
        
        reader.onload = function() {
            const preview = document.getElementById('profile-preview');
            const placeholder = document.getElementById('profile-placeholder');
            
            // Set the source to the file data
            preview.src = reader.result;
            
            // Show image, hide text placeholder if it exists
            preview.classList.remove('hidden');
            if (placeholder) placeholder.style.display = 'none';
        };
        
        // Read the file as a Data URL
        if(input.files && input.files[0]) {
            reader.readAsDataURL(input.files[0]);
        }
    }
</script>

<%- include('./partials/footer') %>